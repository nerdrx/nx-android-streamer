<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The whole app is one WebRTC pipe over a private Tailscale/WireGuard net. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- WakeLock keeps the CPU alive so the stream survives the screen going off. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Watch network transitions so we can force a reconnect on Wi-Fi <-> cellular. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- QR pairing only. Requested at runtime; denial falls back to manual entry. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.NXStreamer"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true">

        <!-- Single activity. Portrait-locked; it renders portrait content and owns
             every touch. keepScreenOn + immersive fullscreen are set in code. -->
        <activity
            android:name=".StreamActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|smallestScreenSize|density"
            android:theme="@style/Theme.NXStreamer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
